export default {
  lists_create_with: {
    // works
    targets: [
      {
        type: 'text',
        fields: [{ field: 'TEXT' }],
        prompt: 'text value',
        repeat: true,
      },
    ],
  },
  logic_compare: {
    targets: [
      {
        type: 'text',
        fields: [{ field: 'TEXT' }],
        prompt: 'text value',
        repeat: true,
      },
    ],
  },
  text_join: {
    // works
    targets: [
      {
        type: 'text',
        fields: [{ field: 'TEXT' }],
        prompt: 'text value',
        repeat: true,
      },
    ],
  },
  lists_repeat: {
    targets: [
      {
        type: 'text',
        fields: [{ field: 'TEXT' }],
        prompt: 'text value',
        repeat: true,
      },
    ],
  },
};
